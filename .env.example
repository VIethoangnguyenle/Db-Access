# Secrets referenced by config.yaml via ${ENV}. Connection details live in config.yaml now.
CONFIG_PATH=./config.yaml

# Example secrets for config.example.yaml:
PROD_USER=system
PROD_PASS=changeme
KEY_A=replace-with-strong-random-key
# SSH_KEY_PATH=/home/user/.ssh/id_rsa

# Optional: HTTP port (default 3000)
# PORT=3000
